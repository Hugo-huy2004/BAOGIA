# AGENTS.md

Hướng dẫn cho agent nằm ở **[CLAUDE.md](CLAUDE.md)** — một nguồn duy nhất cho
mọi công cụ (Claude Code đọc `CLAUDE.md`, Codex đọc file này).

Trước đây hai file là bản sao của nhau, chỉ khác đúng hai chữ ở dòng tiêu đề.
Không ai nhớ sửa cả hai, nên chúng đã lệch nhau — bản `AGENTS.md` thiếu hẳn
đoạn nói cổng backend là 8099 chứ không phải 8081, đúng cái bẫy vừa làm mất
một buổi. Giữ một file là cách duy nhất để chuyện đó không lặp lại.
